// Main container component

// Manages board state and card operations (add, edit, delete, vote)
//Renders board header and columns
// Handles board update

// the board holds drag state (isDraggig, draggedCardType, dragOverColumn)
// handles drag events - onDragStart, onDragEnd, onDrop
// passes drag handlers down to child components as props
// coordinates between card pool and columns